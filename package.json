{
  "name": "langsplain",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive transformer explainer",
  "scripts": {
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  }
}
